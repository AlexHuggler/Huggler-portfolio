---
title: "AI-Assisted SQL Optimizer"
tagline: "CLI that uses Claude to suggest Spark/Snowflake query rewrites and partition strategies. Benchmarked against a 50-query corpus."
order: 3
stack:
  - "Python"
  - "Typer"
  - "Anthropic SDK"
  - "sqlglot"
  - "pytest"
repoUrl: "https://github.com/AlexHuggler/Huggler-portfolio/tree/main/project-ai-sql-optimizer"
results:
  - "50-query corpus across 5 optimization categories with labeled ground truth"
  - "Heuristic analyzer flags select-*, cross joins, missing partition predicates, and broadcast opportunities"
  - "Suggestions scored on keyword overlap, EXPLAIN cost delta, and human review"
---

import MermaidBlock from "../../components/MermaidBlock.astro";

## Problem

There is a real gap between "junior writes a working query" and "senior knows
when to broadcast, partition-prune, flatten a CTE, or rewrite a window
function." This tool closes part of that gap by sending parsed SQL plus
heuristic findings to Claude with a structured optimizer prompt, then
optionally verifying the rewrite against a real EXPLAIN plan. It targets
Spark SQL and Snowflake, the two dialects I work with most.

## How it works

<MermaidBlock code={`sequenceDiagram
  participant U as User
  participant CLI as sql-optimizer CLI
  participant A as Analyzer (sqlglot + heuristics)
  participant C as Claude API
  participant E as EXPLAIN runner (optional)

  U->>CLI: analyze query.sql
  CLI->>A: parse + collect findings
  A-->>CLI: AST + findings
  CLI->>C: optimizer_prompt + sql + findings
  C-->>CLI: rewrite + reasoning
  CLI->>E: EXPLAIN original / EXPLAIN rewrite
  E-->>CLI: cost deltas
  CLI-->>U: markdown diff + cost report
`} />

## Methodology

Fifty queries across five categories: join optimization, aggregation rewrites,
CTE flattening, partition pruning, broadcast-join opportunities. Each is
labeled with expected suggestion keywords and expected cost direction in
`corpus/ground_truth.yaml`. Suggestions are scored on three axes: keyword
overlap with ground truth, EXPLAIN cost delta (when the engine is available),
and human review.

## Results

The benchmark runs the CLI over the 50-query corpus and scores each suggestion on three
axes: keyword overlap with the labeled ground truth in `corpus/ground_truth.yaml`, EXPLAIN
cost delta when an engine is available, and human review. The heuristic analyzer alone
(no API key required) flags `SELECT *`, cross joins, missing partition predicates, broadcast
opportunities, and CTE depth, which is enough to drive the dry-run mode. With
`ANTHROPIC_API_KEY` set, Claude proposes rewrites that the EXPLAIN runner verifies for a
lower cost direction on the five labeled categories.

## Honest limitations

- Claude can hallucinate column or table names that look plausible but do not exist in the schema.
- EXPLAIN-based scoring is only as good as the optimizer's cost estimates - a "cheaper" plan can still be slower in practice.
- Dialect drift between Spark SQL and Snowflake catches edge cases (e.g. `QUALIFY`, lateral views).
- Prompt sensitivity: small changes to the prompt template materially change suggestion quality.
- The CLI runs against `ANTHROPIC_API_KEY`; without it, only heuristic dry-run mode is available.

See the repo for the full corpus, prompt template, and CLI usage.
